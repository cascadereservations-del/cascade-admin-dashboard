# P02 evidence — baseline analytics audit fixtures (re-verified 2026-09-13, read-only)

Window: 2026-01-01 to 2026-10-01 (current year through current month, exclusive end), property `6ae230f4-…`.

| ID | Query (read-only, production) | PRD value | Value 2026-09-13 |
|---|---|---|---|
| A01 legacy | `sum(host_payout)` over confirmed/completed reservations by `coalesce(payout_date, checkout_date)` | ₱275,658.23 | **₱275,658.23** |
| A01 canonical | `sum(gross_amount)` from `v_canonical_ledger` income by `transaction_date` | ₱267,651.19 | **₱269,245.31** (moved by ₱1,594.12 since the PRD; difference now ₱6,412.92) |
| A02 | confirmed income with source not in (airbnb, airbnb_email, airbnb_payout_email) | ₱1,780 | **₱1,780.00** (1 `direct_booking` row) |
| A04 | confirmed/completed reservations with check-in after today entering the YTD basis | 2 res / 4 nights | **2 res / 4 nights** |
| A06 | confirmed/completed reservations with `payout_date is null` | 4 | **3** |
| A09 | rows in `finance_reconciled_facts` / `crm_guest_profiles` / `inventory_stock_movements` | 0 / 0 / 0 | **0 / 0 / 0** |
| A10 | inventory PWA still updates `qty_on_hand` directly (`inventory/index.html` lines 3146 and 3196) while `record_inventory_movement` exists | confirmed | **confirmed** |
| A11 | legacy admin inserts into reservation records for direct bookings | confirmed by source inspection | **confirmed** |
| A12 | nine ledger versions aligned by `20260909140000_ledger_reconciliation` | aligned | **aligned**; three later versions exist in the ledger whose sources live outside stay-site |

Legacy calculation reproduced from `index.html` `loadOverview()` (lines 2222–2300): ADR = host payout ÷ nights (A03), YTD filters by `checkin_date` within the calendar year without excluding future stays (A04), sparklines keyed by `payout_date || checkout_date` while headline KPIs key by `checkin_date` (A05), guest stats by name-derived tier (A07), several queries without `property_id` (A08: `inventory_items`, `cleaning_sessions`, `calendar_sync_log`).

These findings are defects and discrepancies, not audited statements. The new metric service (`get_hospitality_metrics_v1`) and the reconciliation workbench replace the legacy calculation; the fixture suite `stay-site/supabase/tests/database/admin_read_models_v1.sql` encodes the month-boundary, cancelled, future, missing-amount, blocked-night and zero-denominator cases.
