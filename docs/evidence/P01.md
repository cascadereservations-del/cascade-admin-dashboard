# P01 evidence — current-state and migration manifest (2026-09-13)

Read-only inspection of repo state and the live Supabase project `qkgfhsdppslwunarczeq` (property `6ae230f4-c189-4547-84b1-cb6e0b2cc9bd`).

## Repositories
| Repo | HEAD before | Notes |
|---|---|---|
| admin-dashboard | `99e558a` main, clean; branch `codex/admin-site-upgrade-20260910` differs by one test line | legacy `index.html` 302 KB, `index2.html` 282 KB; reads 16 tables and 1 RPC directly; auth checks `app_metadata.role` |
| stay-site | `aae44a4` main | migrations through `20260910080000`; live ledger also holds `20260911000000_concierge_threads`, `20260912000000`, `20260912010000` (sources in `direct-booking-waves-0-1-sol`, not in stay-site) |
| inventory | `8eacecc` | writes `inventory_items.qty_on_hand` directly in two places and calls `record_inventory_usage` |
| CH-Cleaners-Checklist | `2303526` | untouched |

## Live objects classified
- **Deployed and reused:** `current_staff_access`, `current_staff_authorized`, `staff_access_allowed`, `decide_direct_booking`, `record_booking_lifecycle_action`, `authorize_booking_refund`, `review_cleaning_verification`, `record_inventory_movement`, `record_inventory_usage` (v1), `get_payment_review_queue`, `record_payment_finance_review`, `get_data_integrity_report`, `get_management_metrics`, Edge Function `staff-users`, tables `guests`, `airbnb_reservations`, `booking_inquiries`, `calendar_events`, `transactions`, `cleaning_sessions`, `meter_readings`, `cleaning_verification_evidence/reviews`, `inventory_*`, `concierge_handoffs`, `guest_conversations`, `job_heartbeats`, `staff_access_audit`.
- **Deployed, zero rows (A09 confirmed):** `finance_reconciled_facts` 0, `crm_guest_profiles` 0, `inventory_stock_movements` 0.
- **Missing (added locally, not applied):** everything in `docs/CONTRACTS.md` "New v1 interfaces" and "Data additions".
- **Two RLS models coexist:** older tables (`airbnb_reservations`, `calendar_events`, `transactions`, `guests`, `booking_inquiries`, `employees`) key RLS on `auth.jwt()->app_metadata.role`; newer tables use `current_staff_authorized`. The admin therefore requires a user that carries both an `app_metadata.role` of owner/admin and a `staff_access_profiles` row; the new `*_v1` functions are SECURITY DEFINER with explicit capability checks so they do not depend on the old JWT claim.
- **Staff profiles live:** owner, admin, admin, cleaner (all enabled).
- **Row counts:** reservations 110 (97 completed, 3 confirmed, 10 cancelled; all Airbnb; all have `guest_id`), calendar events 171 (31 confirmed, 17 blocked, 120 cancelled airbnb; 4 linked to reservations), transactions 301, guests 94, cleaning sessions 37, inventory items 64, inquiries 3.
- **Tooling on this workstation:** Node 24, npm 11, Deno 2.9.5, Docker CLI present but daemon off, no psql, no Supabase CLI, no Playwright browsers. Consequence: pgTAP suites are written but not run here.
